package com.mx.eventbuspatt.login.utils

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.Dp
import androidx.compose.ui.unit.dp
import java.lang.reflect.Modifier

/*
// Función para redondear las esquinas
fun Modifier.BordesRedondeados(
    radius: Dp = 8.dp,  // Valor predeterminado para el radio
    backgroundColor: Color = Color.Transparent, // Color de fondo opcional
    padding: Dp = 0.dp // Margen opcional
): Modifier {
    return this
        .padding(padding)
        .clip(RoundedCornerShape(radius)) // Redondea las esquinas
        .background(backgroundColor) // Aplica el color de fondo
}*/